---
- name: "nxos.mac: show mac address-table"
  cli_parse_transform:
    engine: "native_json"
    commands:
      - command: "show mac address-table"
        set_fact: True
        transform:
        - name: "nxos_flatten_table_row"

- name: "nxos.mac: assert entry"
  assert:
    that: 
    - "desired mac address entry is present...."
